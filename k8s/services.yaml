apiVersion: v1
kind: Service
metadata:
  name: blueprint-chat-api
  namespace: blueprint-chat
spec:
  selector:
    app: blueprint-chat-api
  ports:
  - name: http
    port: 8080
    targetPort: 8080
